{
	"name": "server",
	"version": "1.0.0",
	"description": "Backend de l'application Quiz-code",
	"main": "server.js",
	"scripts": {
		"dev": "nodemon server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^4.18.2"
	},
	"devDependencies": {
		"dotenv": "^16.4.4",
		"nodemon": "^3.0.3"
	}
}
